[project]
name = "boxbrain-memory-priority-stack"
version = "0.1.0"
description = "Repository-first memory and action-priority resolver for BoxBrain"
requires-python = ">=3.10"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
