name: Deploy Aurum Arkmatx

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'Web/Aurum-Arkmatx/**'
      - '.github/workflows/deploy-aurum-arkmatx.yml'

permissions:
  contents: write

concurrency:
  group: aurum-arkmatx-deploy
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: arkmatx
    steps:
      - uses: actions/checkout@v4

      - name: Detect hosted deployment configuration
        id: config
        env:
          HOST: ${{ secrets.ARKMATX_SSH_HOST }}
          USER: ${{ secrets.ARKMATX_SSH_USER }}
          KEY: ${{ secrets.ARKMATX_SSH_KEY }}
          HOST_KEY: ${{ secrets.ARKMATX_SSH_HOST_KEY }}
          REMOTE_PATH: ${{ secrets.ARKMATX_REMOTE_PATH }}
          PUBLIC_URL: ${{ secrets.ARKMATX_PUBLIC_URL }}
        run: |
          set -euo pipefail
          missing=()
          for name in HOST USER KEY HOST_KEY REMOTE_PATH PUBLIC_URL; do
            if [ -z "${!name:-}" ]; then
              missing+=("$name")
            fi
          done
          if [ "${#missing[@]}" -eq 0 ]; then
            echo 'configured=true' >> "$GITHUB_OUTPUT"
            echo 'missing=none' >> "$GITHUB_OUTPUT"
            echo 'AURUM_WEB_HOSTING configured=true'
          else
            joined="$(IFS=,; printf '%s' "${missing[*]}")"
            echo 'configured=false' >> "$GITHUB_OUTPUT"
            echo "missing=$joined" >> "$GITHUB_OUTPUT"
            echo "AURUM_WEB_HOSTING configured=false missing=$joined"
            echo '::notice::Hosted Arkmatx deployment is not configured. The no-secret repository dashboard and voice mirrors will still be validated and receipted.'
          fi

      - name: Validate no-secret dashboard and voice mirrors
        run: |
          set -euo pipefail
          test -s Web/Aurum-Arkmatx/dashboard.html
          test -s Web/Aurum-Arkmatx/voice-status.txt
          test -s Web/Aurum-Arkmatx/voice-status.json
          test -s Web/Aurum-Arkmatx/voice-status/index.html
          test -s Web/Aurum-Arkmatx/status
          test -s Web/Aurum-Arkmatx/nodes
          test -s AURUM_VOICE_STATUS.md
          grep -F 'Progress is proof, not activity.' Web/Aurum-Arkmatx/dashboard.html >/dev/null
          grep -F 'voice-status.json' Web/Aurum-Arkmatx/dashboard.html >/dev/null
          grep -F 'AURUM VOICE STATUS' Web/Aurum-Arkmatx/voice-status.txt >/dev/null
          grep -F 'Read Aurum Voice Status' AURUM_VOICE_STATUS.md >/dev/null
          python3 - <<'PY'
          import json
          from pathlib import Path

          root = Path('Web/Aurum-Arkmatx')
          payload = json.loads((root / 'voice-status.json').read_text(encoding='utf-8'))
          assert payload['schema'] == 'aurum-voice-status-v1'
          assert payload['overall']['state'] == 'awaiting-boot-proof'
          capabilities = payload['human_capabilities']
          assert len(capabilities) == 7
          expected = {'defined', 'executable', 'tested', 'seeded', 'booted', 'used'}
          for capability in capabilities:
              assert set(capability['stages']) == expected
              assert capability['stages']['defined'] is True
              assert capability['stages']['executable'] is True
              assert capability['stages']['tested'] is True
              assert capability['stages']['seeded'] is True
              assert capability['stages']['booted'] is False
              assert capability['stages']['used'] is False
          status = json.loads((root / 'status').read_text(encoding='utf-8'))
          nodes = json.loads((root / 'nodes').read_text(encoding='utf-8'))
          assert status['status'] == 'read-only-static-status-mirror'
          assert isinstance(nodes['nodes'], list)
          print('AURUM_STATIC_MIRRORS_OK capabilities=7 floor=4/6 next=booted')
          PY

      - name: Configure pinned SSH identity
        if: steps.config.outputs.configured == 'true'
        env:
          KEY: ${{ secrets.ARKMATX_SSH_KEY }}
          HOST_KEY: ${{ secrets.ARKMATX_SSH_HOST_KEY }}
        run: |
          set -euo pipefail
          install -d -m 700 ~/.ssh
          printf '%s\n' "$KEY" > ~/.ssh/id_ed25519
          chmod 600 ~/.ssh/id_ed25519
          printf '%s\n' "$HOST_KEY" > ~/.ssh/known_hosts
          chmod 600 ~/.ssh/known_hosts

      - name: Verify host and PHP
        if: steps.config.outputs.configured == 'true'
        env:
          HOST: ${{ secrets.ARKMATX_SSH_HOST }}
          USER: ${{ secrets.ARKMATX_SSH_USER }}
          PORT: ${{ secrets.ARKMATX_SSH_PORT }}
          REMOTE_PATH: ${{ secrets.ARKMATX_REMOTE_PATH }}
        run: |
          set -euo pipefail
          PORT="${PORT:-22}"
          ssh -p "$PORT" -o BatchMode=yes -o IdentitiesOnly=yes "$USER@$HOST" \
            "php -v >/dev/null && mkdir -p -- '$REMOTE_PATH'"

      - name: Upload web node
        if: steps.config.outputs.configured == 'true'
        env:
          HOST: ${{ secrets.ARKMATX_SSH_HOST }}
          USER: ${{ secrets.ARKMATX_SSH_USER }}
          PORT: ${{ secrets.ARKMATX_SSH_PORT }}
          REMOTE_PATH: ${{ secrets.ARKMATX_REMOTE_PATH }}
        run: |
          set -euo pipefail
          PORT="${PORT:-22}"
          tar -C Web/Aurum-Arkmatx -czf /tmp/aurum-arkmatx.tgz .
          scp -P "$PORT" -o BatchMode=yes -o IdentitiesOnly=yes /tmp/aurum-arkmatx.tgz "$USER@$HOST:/tmp/aurum-arkmatx.tgz"
          ssh -p "$PORT" -o BatchMode=yes -o IdentitiesOnly=yes "$USER@$HOST" \
            "set -eu; mkdir -p -- '$REMOTE_PATH'; tar -xzf /tmp/aurum-arkmatx.tgz -C '$REMOTE_PATH'; rm -f /tmp/aurum-arkmatx.tgz; php -l '$REMOTE_PATH/index.php'; php -l '$REMOTE_PATH/voice-status.php'"

      - name: Verify public node, dashboard, and voice mirror
        if: steps.config.outputs.configured == 'true'
        env:
          URL: ${{ secrets.ARKMATX_PUBLIC_URL }}
        run: |
          set -euo pipefail
          base="${URL%/}"

          body="$(curl --fail --silent --show-error --max-time 10 "$base/status")"
          printf '%s' "$body" | grep -F 'Aurum-Arkmatx' >/dev/null
          printf '%s' "$body" | grep -F 'active-edge-web-node' >/dev/null

          dashboard="$(curl --fail --silent --show-error --max-time 10 "$base/dashboard")"
          printf '%s' "$dashboard" | grep -F 'Progress is proof, not activity.' >/dev/null
          printf '%s' "$dashboard" | grep -F 'voice-status.json' >/dev/null

          voice="$(curl --fail --silent --show-error --max-time 15 "$base/voice-status")"
          printf '%s' "$voice" | grep -F 'AURUM VOICE STATUS' >/dev/null
          printf '%s' "$voice" | grep -F 'Human capabilities:' >/dev/null

          curl --fail --silent --show-error --max-time 15 "$base/voice-status.json" > /tmp/aurum-voice-status.json
          python3 - <<'PY'
          import json
          from pathlib import Path

          payload = json.loads(Path('/tmp/aurum-voice-status.json').read_text())
          assert payload['schema'] == 'aurum-voice-status-v1'
          capabilities = payload['human_capabilities']
          assert len(capabilities) == 7
          expected = {'defined', 'executable', 'tested', 'seeded', 'booted', 'used'}
          for capability in capabilities:
              assert set(capability['stages']) == expected
          print(
              'AURUM_VOICE_MIRROR_OK '
              f"source={payload.get('source')} "
              f"capabilities={len(capabilities)}"
          )
          PY

      - name: Observe bounded BBPI4 AP probes
        if: steps.config.outputs.configured == 'true'
        env:
          URL: ${{ secrets.ARKMATX_PUBLIC_URL }}
        run: |
          set -euo pipefail
          python3 - <<'PY'
          import json
          import os
          import time
          import urllib.parse
          import urllib.request

          base = os.environ["URL"].rstrip("/")
          work_ids = (
              "bbpi4-ap-probe-morris-v1",
              "bbpi4-ap-probe-bruce-v1",
          )
          latest = {}
          deadline = time.time() + 90
          while time.time() < deadline:
              for work_id in work_ids:
                  url = base + "/work/status?id=" + urllib.parse.quote(work_id)
                  try:
                      with urllib.request.urlopen(url, timeout=10) as response:
                          latest[work_id] = json.load(response)
                  except Exception as exc:
                      latest[work_id] = {
                          "work_id": work_id,
                          "status": "unavailable",
                          "attempts": 0,
                          "ap_ssh_open": False,
                          "error_type": type(exc).__name__,
                      }
              if all(latest.get(work_id, {}).get("status") == "completed" for work_id in work_ids):
                  break
              time.sleep(5)

          for work_id in work_ids:
              item = latest.get(work_id, {})
              print(
                  "AURUM_AP_PROBE "
                  f"work_id={work_id} "
                  f"status={item.get('status', 'missing')} "
                  f"attempts={int(item.get('attempts') or 0)} "
                  f"ap_ssh_open={str(bool(item.get('ap_ssh_open'))).lower()} "
                  f"result={item.get('last_result_status') or 'none'}"
              )
          PY

      - name: Publish hosted dashboard and voice mirror receipt
        if: steps.config.outputs.configured == 'true'
        env:
          GH_TOKEN: ${{ github.token }}
          PUBLIC_URL: ${{ secrets.ARKMATX_PUBLIC_URL }}
        run: |
          set -euo pipefail
          python3 - <<'PY'
          import base64
          import json
          import os
          import urllib.request
          from datetime import datetime, timezone

          repo = os.environ['GITHUB_REPOSITORY']
          run_id = os.environ['GITHUB_RUN_ID']
          attempt = os.environ['GITHUB_RUN_ATTEMPT']
          path = f'Projects/AurumBridge/results/web-mirror-{run_id}-attempt-{attempt}.json'
          payload = {
              'schema': 'aurum-web-mirror-receipt-v1',
              'state': 'WEB_MIRROR_OK',
              'github_workflow_run': int(run_id),
              'github_run_attempt': int(attempt),
              'source_commit': os.environ['GITHUB_SHA'],
              'observed_at': datetime.now(timezone.utc).isoformat(),
              'public_base': os.environ['PUBLIC_URL'].rstrip('/'),
              'verified': {
                  'dashboard_truth_banner': True,
                  'dashboard_reads_voice_mirror': True,
                  'plain_text_voice_status': True,
                  'json_voice_status': True,
                  'seven_human_capabilities': True,
                  'six_evidence_gates': True,
                  'php_lint': True,
              },
          }
          encoded = base64.b64encode(
              (json.dumps(payload, indent=2) + '\n').encode('utf-8')
          ).decode('ascii')
          body = json.dumps({
              'message': 'Record Aurum dashboard voice mirror deploy state=WEB_MIRROR_OK [skip ci]',
              'content': encoded,
              'branch': 'main',
          }).encode('utf-8')
          request = urllib.request.Request(
              f'https://api.github.com/repos/{repo}/contents/{path}',
              data=body,
              method='PUT',
              headers={
                  'Authorization': f"Bearer {os.environ['GH_TOKEN']}",
                  'Accept': 'application/vnd.github+json',
                  'X-GitHub-Api-Version': '2022-11-28',
                  'Content-Type': 'application/json',
              },
          )
          with urllib.request.urlopen(request, timeout=30) as response:
              if response.status not in (200, 201):
                  raise SystemExit(f'unexpected receipt status {response.status}')
          print(f'AURUM_WEB_MIRROR_RECEIPT path={path} state=WEB_MIRROR_OK')
          PY

      - name: Publish no-secret static mirror receipt
        if: steps.config.outputs.configured != 'true'
        env:
          GH_TOKEN: ${{ github.token }}
          MISSING: ${{ steps.config.outputs.missing }}
        run: |
          set -euo pipefail
          python3 - <<'PY'
          import base64
          import json
          import os
          import urllib.request
          from datetime import datetime, timezone

          repo = os.environ['GITHUB_REPOSITORY']
          run_id = os.environ['GITHUB_RUN_ID']
          attempt = os.environ['GITHUB_RUN_ATTEMPT']
          path = f'Projects/AurumBridge/results/web-static-mirror-{run_id}-attempt-{attempt}.json'
          payload = {
              'schema': 'aurum-web-static-mirror-receipt-v1',
              'state': 'WEB_STATIC_MIRROR_OK',
              'github_workflow_run': int(run_id),
              'github_run_attempt': int(attempt),
              'source_commit': os.environ['GITHUB_SHA'],
              'observed_at': datetime.now(timezone.utc).isoformat(),
              'hosted_deployment': {
                  'configured': False,
                  'missing': [item for item in os.environ.get('MISSING', '').split(',') if item],
              },
              'public_sources': {
                  'text': 'https://raw.githubusercontent.com/FormatX66/BoxBrain/main/Web/Aurum-Arkmatx/voice-status.txt',
                  'json': 'https://raw.githubusercontent.com/FormatX66/BoxBrain/main/Web/Aurum-Arkmatx/voice-status.json',
                  'repository': 'https://github.com/FormatX66/BoxBrain/blob/main/AURUM_VOICE_STATUS.md',
              },
              'verified': {
                  'dashboard_truth_banner': True,
                  'dashboard_reads_static_json': True,
                  'plain_text_voice_status': True,
                  'json_voice_status': True,
                  'browser_static_voice_view': True,
                  'seven_human_capabilities': True,
                  'six_evidence_gates': True,
                  'seeded_floor': '4/6',
              },
          }
          encoded = base64.b64encode(
              (json.dumps(payload, indent=2) + '\n').encode('utf-8')
          ).decode('ascii')
          body = json.dumps({
              'message': 'Record Aurum static dashboard voice mirror state=WEB_STATIC_MIRROR_OK [skip ci]',
              'content': encoded,
              'branch': 'main',
          }).encode('utf-8')
          request = urllib.request.Request(
              f'https://api.github.com/repos/{repo}/contents/{path}',
              data=body,
              method='PUT',
              headers={
                  'Authorization': f"Bearer {os.environ['GH_TOKEN']}",
                  'Accept': 'application/vnd.github+json',
                  'X-GitHub-Api-Version': '2022-11-28',
                  'Content-Type': 'application/json',
              },
          )
          with urllib.request.urlopen(request, timeout=30) as response:
              if response.status not in (200, 201):
                  raise SystemExit(f'unexpected receipt status {response.status}')
          print(f'AURUM_WEB_STATIC_MIRROR_RECEIPT path={path} state=WEB_STATIC_MIRROR_OK')
          PY
