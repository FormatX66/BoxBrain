/* AURUM_OPERATOR_COMMANDS_V1_CANONICAL
 * Canonical website owner: FormatX66/ClusterSites.
 * Surfaces only repository-backed commands; Proposed commands are never presented as runnable.
 */
(()=>{'use strict';if(window.__aurumOperatorCommandsV1)return;window.__aurumOperatorCommandsV1=true;
const RAW='https://raw.githubusercontent.com/FormatX66/BoxBrain/main/docs/AURUM_COMMANDS.md';
const $=(s,r=document)=>r.querySelector(s),esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
let commands=[],error='';
const style=document.createElement('style');style.textContent=`.cc32-summary{grid-template-columns:repeat(7,minmax(0,1fr))!important}.aurum-command-row code{font:700 11px ui-monospace,SFMono-Regular,Menlo,monospace;color:#d8d4ff}.aurum-command-row .proposed{background:#3a2e16;color:#f0c76a}.aurum-command-row .available{background:#173428;color:#8ce7b2}.aurum-command-links{display:flex;gap:8px;flex-wrap:wrap;margin-top:12px}.aurum-command-links a{min-height:36px;display:inline-flex;align-items:center;padding:7px 10px;border:1px solid #363c4c;border-radius:9px;background:#171b25;color:#dfe1ff;text-decoration:none;font-size:10px;font-weight:760}@media(max-width:1180px){.cc32-summary{grid-template-columns:repeat(4,minmax(0,1fr))!important}}@media(max-width:700px){.cc32-summary{grid-template-columns:repeat(2,minmax(0,1fr))!important}}`;document.head.appendChild(style);
function parse(text){return text.split(/\r?\n/).map(line=>{if(!/^\|\s*`/.test(line))return null;const p=line.split('|').slice(1,-1).map(x=>x.trim());if(p.length<3)return null;const command=(p[0].match(/`([^`]+)`/)||[])[1];if(!command)return null;return{command,scope:p[1],state:p[2],evidence:p[3]||''}}).filter(Boolean)}
function ready(){return $('.cc32-summary')&&$('.cc32-drawer')}
function metric(){let m=$('#aurumCommandsMetric');if(m)return m;const sum=$('.cc32-summary');if(!sum)return null;m=document.createElement('button');m.id='aurumCommandsMetric';m.className='cc32-metric';m.type='button';m.setAttribute('aria-expanded','false');m.innerHTML='<strong id="aurumCommandsCount">—</strong><span>operator commands</span>';sum.appendChild(m);m.addEventListener('click',()=>show(m));return m}
function show(m){const drawer=$('.cc32-drawer');if(!drawer)return;const available=commands.filter(x=>!/^proposed$/i.test(x.state)),proposed=commands.filter(x=>/^proposed$/i.test(x.state));const rows=commands.map(x=>`<div class="cc32-row aurum-command-row"><b><code>${esc(x.command)}</code></b><span class="cc32-owner ${/^proposed$/i.test(x.state)?'proposed':'available'}">${esc(x.state)}</span><p>${esc(x.scope)}${x.evidence?`<br>${esc(x.evidence)}`:''}</p></div>`).join('');drawer.innerHTML=`<div class="cc32-head"><div><p class="eyebrow">COMMAND REGISTRY</p><h2>Operator Commands</h2><p>${error?esc(error):`${available.length} repository-backed command${available.length===1?'':'s'} are documented for their exact scope. ${proposed.length} Aurum-native maintenance command${proposed.length===1?' is':'s are'} still proposed and must not be treated as runnable.`}</p></div><button class="cc32-close" type="button">×</button></div>${rows?`<div class="cc32-list">${rows}</div>`:'<div class="cc32-empty">Command registry evidence is unavailable. Do not invent a replacement command; this is a system evidence gap, not a human task.</div>'}<div class="aurum-command-links"><a href="https://github.com/FormatX66/BoxBrain/blob/main/docs/AURUM_COMMANDS.md" target="_blank" rel="noopener">Open canonical command registry ↗</a></div>`;drawer.classList.add('show');document.querySelectorAll('.cc32-metric').forEach(x=>x.setAttribute('aria-expanded',String(x===m)));$('.cc32-close',drawer).onclick=()=>{drawer.classList.remove('show');document.querySelectorAll('.cc32-metric').forEach(x=>x.setAttribute('aria-expanded','false'))}}
async function load(){try{const r=await fetch(`${RAW}?t=${Date.now()}`,{cache:'no-store'});if(!r.ok)throw new Error(`HTTP ${r.status}`);commands=parse(await r.text());if(!commands.length)throw new Error('registry contained no command rows');error=''}catch(e){commands=[];error=`Command registry unavailable (${e.message||e}).`}const m=metric();const available=commands.filter(x=>!/^proposed$/i.test(x.state));const count=$('#aurumCommandsCount');if(count)count.textContent=commands.length?String(available.length):'—';if(m)m.title=commands.length?`${available.length} repository-backed commands; ${commands.length-available.length} proposed`:'Registry unavailable'}
function boot(){if(!ready()){setTimeout(boot,250);return}metric();load();setInterval(load,5*60*1000)}boot();
})();
