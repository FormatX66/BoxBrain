/* AURUM_PI3_PHYSICAL_EVIDENCE_V1_4_CANONICAL
 * AURUM_PI3_PHYSICAL_EVIDENCE_V1_3_CANONICAL compatibility marker.
 * AURUM_PI3_PHYSICAL_EVIDENCE_V1_2_CANONICAL compatibility marker.
 * AURUM_PI3_PHYSICAL_EVIDENCE_V1_CANONICAL compatibility marker.
 * Compatibility literal for older validators: schema:'aurum-command-center-pi3-physical-evidence-v1.3'
 * Canonical website owner: FormatX66/ClusterSites.
 * Read-only physical-evidence surface for the dedicated Aurum Raspberry Pi 3 experiment node.
 * Physical userspace adaptive-driver proof does not imply a kernel canary, production ARM64 proof,
 * mutation authority, promotion authority, root cause, or a human task.
 */
(()=>{'use strict';if(window.__aurumPi3PhysicalEvidenceV1)return;window.__aurumPi3PhysicalEvidenceV1=true;
const RAW='https://raw.githubusercontent.com/FormatX66/BoxBrain/main';
const API='https://api.github.com/repos/FormatX66/BoxBrain';
const REQUEST_URL=`${RAW}/Projects/Aurum/Experiments/pi3-probe-request.json`;
const RECEIPT_URL=`${RAW}/Projects/Aurum/Experiments/latest-pi3-physical-receipt.json`;
const PROBE_URL=`${RAW}/Projects/Aurum/Experiments/pi3_physical_probe.py`;
const PROBE_WORKFLOW_URL=`${RAW}/.github/workflows/aurum-pi3-readonly-probe.yml`;
const EXP_WORKFLOW_URL=`${RAW}/.github/workflows/aurum-experiments.yml`;
const ATTACHED_REQUEST_URL=`${RAW}/Projects/Aurum/Experiments/pi3-global-runner-request.json`;
const ATTACHED_RECEIPT_URL=`${RAW}/Projects/Aurum/Experiments/latest-pi3-attached-runner-probe.json`;
const ATTACHED_WORKFLOW_URL=`${RAW}/.github/workflows/aurum-pi3-global-runner-probe.yml`;
const GEN2_URL=`${RAW}/Projects/AdaptiveDrivers/evidence/pi3-generation2-physical.json`;
const KERNEL_PREFLIGHT_REQUEST_URL=`${RAW}/Projects/AdaptiveDrivers/requests/pi3-kernel-canary-preflight.json`;
const KERNEL_PREFLIGHT_WORKFLOW_URL=`${RAW}/.github/workflows/aurum-pi3-kernel-canary-preflight.yml`;
const PROBE_WORKFLOW='Aurum Pi3 Read-Only Physical Probe';
const ATTACHED_WORKFLOW='Aurum Pi3 Attached-Runner Read-Only Probe';
const EXP_WORKFLOW='Aurum Parallel Experiments';
const KERNEL_PREFLIGHT_WORKFLOW='Aurum Pi3 Kernel Canary Preflight';
const ATTACHED_RUNNER='GLOBAL-FAMILY-DESKTOP';
const REFRESH=2*60*1000,FRESH=6*60*60*1000;
const $=(s,r=document)=>r.querySelector(s),esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const age=v=>{const n=Date.parse(v||'');return Number.isFinite(n)?Math.max(0,Date.now()-n):null};
const ageText=ms=>{if(ms===null)return'unknown';const m=Math.floor(ms/60000);if(m<60)return`${m} min`;const h=Math.floor(m/60),r=m%60;return r?`${h}h ${r}m`:`${h}h`};
const active=r=>['queued','in_progress','waiting','requested','pending'].includes(String(r?.status||''));
const failed=r=>['failure','timed_out','startup_failure','action_required'].includes(String(r?.conclusion||''));
let state={phase:'checking',request:null,receipt:null,probeText:'',probeWorkflowText:'',expWorkflowText:'',attachedRequest:null,attachedReceipt:null,attachedWorkflowText:'',gen2:null,kernelPreflightRequest:null,kernelPreflightWorkflowText:'',probeRun:null,probeRunAge:null,attachedRun:null,attachedRunAge:null,expRun:null,expRunAge:null,kernelPreflightRun:null,kernelPreflightRunAge:null,error:''};
const safeRequest=()=>Boolean(state.request?.schema==='aurum-pi3-probe-request-v1'&&state.request?.mode==='known-name-read-only'&&state.request?.mutation_allowed===false&&state.request?.promotion_allowed===false&&state.request?.write_authority===false);
const attachedRequestSafe=()=>Boolean(state.attachedRequest?.schema==='aurum-pi3-attached-runner-request-v1'&&state.attachedRequest?.expected_runner_name===ATTACHED_RUNNER&&state.attachedRequest?.active_network_scan===false&&state.attachedRequest?.persistent_trust_change_allowed===false&&state.attachedRequest?.mutation_allowed===false&&state.attachedRequest?.promotion_allowed===false&&state.attachedRequest?.write_authority===false);
const codeGatePresent=()=>['capture-stateweave-before-change','probe-mesh-read-only','stage-adaptive-kernel-candidate-observe-only','keep-adaptive-kernel-held-on-current-arch','promotion_allowed'].every(x=>state.probeText.includes(x));
const workflowGuarded=()=>state.probeWorkflowText.includes('runs-on: [self-hosted, Windows, X64, aurum-elevated]')&&state.probeWorkflowText.includes('write_authority = $false')&&state.probeWorkflowText.includes('mutation_allowed = $false')&&state.probeWorkflowText.includes('promotion_allowed = $false')&&state.probeWorkflowText.includes('BatchMode=yes');
const attachedWorkflowGuarded=()=>['name: Aurum Pi3 Attached-Runner Read-Only Probe','runs-on: [self-hosted, Windows, X64, aurum-elevated]',"$expectedRunner = 'GLOBAL-FAMILY-DESKTOP'",'active_network_scan=$false','persistent_trust_changed=$false','mutation_allowed=$false','promotion_allowed=$false','write_authority=$false','BatchMode=yes','HostKeyAlias=$alias'].every(x=>state.attachedWorkflowText.includes(x));
const experimentGatePresent=()=>state.expWorkflowText.includes('pi3-physical-gate:')&&state.expWorkflowText.includes('test_pi3_physical_probe');
const receiptCaptured=()=>Boolean(state.receipt?.schema==='aurum-pi3-read-only-probe-result-v1'&&state.receipt?.state==='PHYSICAL_RECEIPT_CAPTURED'&&state.receipt?.receipt?.schema==='aurum-pi3-physical-receipt-v1');
const noReachableReceipt=()=>Boolean(state.receipt?.schema==='aurum-pi3-read-only-probe-result-v1'&&state.receipt?.state==='NO_REACHABLE_PI3_RECEIPT'&&state.receipt?.receipt===null&&state.receipt?.mutation_allowed===false&&state.receipt?.promotion_allowed===false&&state.receipt?.write_authority===false);
const attachedReceiptCaptured=()=>Boolean(state.attachedReceipt?.schema==='aurum-pi3-attached-runner-probe-v1'&&state.attachedReceipt?.runner_name===ATTACHED_RUNNER&&state.attachedReceipt?.state==='PHYSICAL_RECEIPT_CAPTURED'&&state.attachedReceipt?.receipt?.schema==='aurum-pi3-physical-receipt-v1'&&state.attachedReceipt?.mutation_allowed===false&&state.attachedReceipt?.promotion_allowed===false&&state.attachedReceipt?.write_authority===false);
const attachedIdentityBoundary=()=>Boolean(state.attachedReceipt?.schema==='aurum-pi3-attached-runner-probe-v1'&&state.attachedReceipt?.runner_name===ATTACHED_RUNNER&&state.attachedReceipt?.state==='REACHABLE_IDENTITY_UNVERIFIED'&&state.attachedReceipt?.receipt===null&&state.attachedReceipt?.mutation_allowed===false&&state.attachedReceipt?.promotion_allowed===false&&state.attachedReceipt?.write_authority===false);
const attachedNoEndpoint=()=>Boolean(state.attachedReceipt?.schema==='aurum-pi3-attached-runner-probe-v1'&&state.attachedReceipt?.runner_name===ATTACHED_RUNNER&&state.attachedReceipt?.state==='NO_REACHABLE_PASSIVE_USB_ENDPOINT'&&state.attachedReceipt?.receipt===null&&state.attachedReceipt?.mutation_allowed===false&&state.attachedReceipt?.promotion_allowed===false&&state.attachedReceipt?.write_authority===false);
const gen2Valid=()=>{const g=state.gen2;return Boolean(g?.schema==='aurum.pi3.adaptive-driver.generation2.physical.v1'&&g?.state==='passed-physical-userspace-generation2'&&g?.generation2?.decision==='promoted'&&Number(g?.generation2?.score)===100&&g?.generation2?.read_evidence_complete===true&&g?.generation2?.lkg_preserved_during_test===true&&g?.fault_injection?.decision==='quarantined'&&g?.fault_injection?.reason==='required-read-only-field-unavailable'&&g?.fault_injection?.lkg_preserved_during_test===true&&g?.fault_injection?.lkg_sha256_before===g?.fault_injection?.lkg_sha256_after&&g?.isolated_metadata_rollback?.passed===true&&g?.safety?.system_driver_changed===false&&g?.safety?.kernel_module_loaded===false&&g?.safety?.kernel_driver_binding_changed===false&&g?.safety?.firmware_mutation_allowed===false&&g?.safety?.kernel_driver_mutation_allowed===false&&g?.safety?.production_nodes_allowed===false&&g?.safety?.persistent_trust_changed===false)};
const kernelPreflightRequestSafe=()=>Boolean(state.kernelPreflightRequest?.schema==='aurum-pi3-kernel-canary-preflight-request-v1'&&state.kernelPreflightRequest?.allowed?.strict_read_only_identity_probe===true&&state.kernelPreflightRequest?.allowed?.compile_inert_module_in_tmp===true&&state.kernelPreflightRequest?.forbidden?.load_kernel_module===true&&state.kernelPreflightRequest?.forbidden?.change_driver_binding===true&&state.kernelPreflightRequest?.forbidden?.change_firmware===true&&state.kernelPreflightRequest?.forbidden?.change_boot_state===true&&state.kernelPreflightRequest?.forbidden?.change_persistent_trust===true&&state.kernelPreflightRequest?.forbidden?.grant_kernel_mutation_authority===true);
const kernelPreflightWorkflowGuarded=()=>['name: Aurum Pi3 Kernel Canary Preflight','runs-on: [self-hosted, Windows, X64, aurum-elevated]','StrictHostKeyChecking=yes','$outOfBandWatchdogProven = $false','$explicitKernelMutationAuthority = $false','kernel_module_load_allowed = $false','driver_binding_change_allowed = $false','firmware_mutation_allowed = $false','module_loaded = $false','system_driver_changed = $false'].every(x=>state.kernelPreflightWorkflowText.includes(x));
const attempts=()=>Array.isArray(state.receipt?.attempts)?state.receipt.attempts:[];
const attachedAttempts=()=>Array.isArray(state.attachedReceipt?.attempts)?state.attachedReceipt.attempts:[];
const latestRun=(runs,name)=>(runs||[]).filter(r=>String(r?.name||r?.display_title||'')===name).sort((a,b)=>new Date(b.updated_at||b.created_at)-new Date(a.updated_at||a.created_at))[0]||null;
function classify(){if(gen2Valid())return'generation2-physical';const implemented=safeRequest()&&codeGatePresent()&&workflowGuarded()&&experimentGatePresent();if(!implemented)return'unknown';if(attachedReceiptCaptured()||receiptCaptured())return'captured';const attachedFresh=state.attachedRunAge!==null&&state.attachedRunAge<=FRESH;if(state.attachedRun&&attachedFresh&&active(state.attachedRun))return'runner-routing';if(attachedIdentityBoundary())return'identity-boundary';if(attachedNoEndpoint())return'attached-unreachable';if(attachedRequestSafe()&&attachedWorkflowGuarded())return'runner-routing-armed';const fresh=state.probeRunAge!==null&&state.probeRunAge<=FRESH;if(state.probeRun&&fresh&&active(state.probeRun))return'running';if(state.probeRun&&fresh&&failed(state.probeRun))return'attention';if(noReachableReceipt())return'unreachable';return'armed'}
function currentReceipt(){return attachedReceiptCaptured()?state.attachedReceipt?.receipt:state.receipt?.receipt||{}}
function runText(r,ms){if(!r)return'no current workflow evidence';return`${r.conclusion||r.status||'unknown'} · ${ageText(ms)} old`}
function kernelGateText(){if(gen2Valid())return'Generation 2 is physically proven only in userspace; kernel-module mutation remains held behind compile/recovery preflight, an automatic out-of-band watchdog, and fresh explicit kernel authority.';if(!attachedReceiptCaptured()&&!receiptCaptured())return'waiting for a real Pi3 identity/hardware receipt before architecture-dependent candidate logic is trusted';const a=String(currentReceipt()?.arch||'').toLowerCase();if(a==='aarch64')return'AArch64 receipt may stage arm64-small observe-only; promotion remains forbidden';if(a==='armv7l'||a==='armv7')return'ARMv7 receipt keeps Adaptive Kernel held on the current observed architecture; no candidate promotion';return`architecture ${a||'unknown'} is recorded, but no candidate promotion is inferred`}
function kernelPreflightText(){const r=state.kernelPreflightRun;if(!r)return kernelPreflightRequestSafe()&&kernelPreflightWorkflowGuarded()?'safe compile-only preflight is armed; no terminal run evidence yet':'not safely proven';if(active(r))return`${r.status} · compile-only / zero mutation authority`;if(r.status==='completed'&&r.conclusion==='success')return'success · prerequisite evidence produced, but kernel load/mutation authority remains false';if(failed(r))return`${r.conclusion} · diagnose without weakening identity/recovery gates`;return`${r.conclusion||r.status||'unknown'} · no mutation authority inferred`}
function attemptText(){const a=attempts();if(!a.length)return'none recorded';return a.map(x=>`${x.target_name||'unknown'}:tcp22=${x.tcp22_reachable===true?'yes':'no'}`).join(' · ')}
function attachedAttemptText(){const a=attachedAttempts();if(!a.length)return'none recorded';return a.map(x=>`${x.endpoint_label||'endpoint'}:${x.trusted_alias||'alias'}:ssh=${x.strict_ssh_exit??'?'}`).join(' · ')}
const css=document.createElement('style');css.textContent=`.pi3-physical-card .p3-hint{margin-top:9px;font-size:9px;color:#737e92;font-weight:750}.pi3-physical-card .p3-detail{display:none;margin-top:11px;padding-top:11px;border-top:1px solid #283041;font-size:10.5px;line-height:1.5;color:#909bad}.pi3-physical-card[aria-expanded=true] .p3-detail{display:block}.pi3-physical-card[aria-expanded=true] .p3-hint{color:#74d8d0}.pi3-physical-card .p3-grid{display:grid;grid-template-columns:155px minmax(0,1fr);gap:5px 9px;margin-top:8px}.pi3-physical-card .p3-grid b{color:#cfd3df;font-size:10px}.pi3-physical-card .p3-grid span{min-width:0;overflow-wrap:anywhere}@media(max-width:680px){.pi3-physical-card .p3-grid{grid-template-columns:1fr}}`;document.head.appendChild(css);
function ensure(){const systems=$('#systems');if(!systems)return null;let card=$('[data-id="pi3-physical-evidence"]',systems);if(card)return card;card=document.createElement('article');card.className='system-card pi3-physical-card';card.dataset.id='pi3-physical-evidence';card.tabIndex=0;card.setAttribute('role','button');card.setAttribute('aria-expanded','false');card.innerHTML='<div class="card-head"><div class="card-icon">π3</div><span class="pill experiment">Experiment</span></div><h3>Pi3 Physical Evidence</h3><p>Real-hardware proof lane for StateWeave, Adaptive Drivers, kernel canaries, mesh probing, and Future Branch — fail closed.</p><div class="evidence">Checking Pi3 physical evidence…</div><div class="p3-hint">Tap to expand physical proof, quarantine/LKG evidence, kernel gate, workflow, and action boundaries →</div><div class="p3-detail"></div>';const toggle=e=>{if(e?.type==='keydown'&&!['Enter',' '].includes(e.key))return;if(e?.type==='keydown')e.preventDefault();card.setAttribute('aria-expanded',String(card.getAttribute('aria-expanded')!=='true'));e?.stopPropagation?.()};card.addEventListener('click',toggle);card.addEventListener('keydown',toggle);systems.appendChild(card);return card}
async function getJson(u,optional=false){const r=await fetch(u,{cache:'no-store',headers:{Accept:'application/vnd.github+json'}});if(optional&&r.status===404)return null;if(!r.ok)throw Error(`HTTP ${r.status}`);return r.json()}
async function getText(u,optional=false){const r=await fetch(u,{cache:'no-store'});if(optional&&r.status===404)return'';if(!r.ok)throw Error(`HTTP ${r.status}`);return r.text()}
function render(){const card=ensure();if(!card)return;state.phase=classify();const pill=$('.pill',card),evidence=$('.evidence',card),detail=$('.p3-detail',card),captured=attachedReceiptCaptured()||receiptCaptured(),unreachable=noReachableReceipt(),attachedUnreachable=attachedNoEndpoint(),identityBoundary=attachedIdentityBoundary(),rr=currentReceipt(),g=state.gen2||{},a=attempts(),gen2=gen2Valid();card.dataset.pi3PhysicalPhase=state.phase;
 if(state.phase==='checking'){pill.className='pill running';pill.textContent='Checking';evidence.textContent='Checking Pi3 physical proof, quarantine/LKG evidence, kernel preflight, topology, and authority boundaries…'}
 else if(state.phase==='generation2-physical'){pill.className='pill experiment';pill.textContent='Physical Proof';evidence.textContent=`Generation 2 userspace adaptive driver physically proven · ${g.generation2?.candidate_id||'candidate'} · score ${g.generation2?.score??'?'} · fault quarantine + LKG metadata preservation + rollback passed · kernel mutation still held.`}
 else if(state.phase==='runner-routing'){pill.className='pill running';pill.textContent='Advancing';evidence.textContent=`Attached-desktop Pi3 proof is ${state.attachedRun?.status||'active'} on the self-hosted Windows lane; only ${ATTACHED_RUNNER} may collect passive USB + strict-SSH evidence.`}
 else if(state.phase==='runner-routing-armed'){pill.className='pill experiment';pill.textContent='Advancing';evidence.textContent=`Attached-runner routing is armed for ${ATTACHED_RUNNER}; zero active scan, trust mutation, write, mutation, or promotion authority.`}
 else if(state.phase==='identity-boundary'){pill.className='pill failed';pill.textContent='Needs Work';evidence.textContent=`${ATTACHED_RUNNER} found a passive USB-side TCP/22 candidate, but strict existing SSH identity/authentication did not verify it. Do not weaken host-key or authentication checks.`}
 else if(state.phase==='attached-unreachable'){pill.className='pill failed';pill.textContent='Needs Work';evidence.textContent=`${ATTACHED_RUNNER} completed the attached-runner probe without a reachable passive USB TCP/22 endpoint. This is a machine-side topology boundary, not proof of a human-only fault.`}
 else if(state.phase==='running'){pill.className='pill running';pill.textContent='Advancing';evidence.textContent=`Read-only Pi3 known-name probe is ${state.probeRun?.status||'active'} on the self-hosted Windows lane · zero write, mutation, or promotion authority.`}
 else if(state.phase==='attention'){pill.className='pill failed';pill.textContent='Needs Work';evidence.textContent=`Pi3 read-only lane is implemented, but the latest fresh known-name probe is ${state.probeRun?.conclusion||state.probeRun?.status}. Root cause is not inferred; Aurum/System owns diagnosis.`}
 else if(state.phase==='captured'){pill.className='pill experiment';pill.textContent='Advancing';evidence.textContent=`Real Pi3 identity/hardware receipt captured · ${rr.model||'Pi3'} · ${rr.arch||'arch unknown'} · ${rr.cores??'?'} cores · ${rr.ram_mb??'?'} MB · observe-only boundary preserved.`}
 else if(state.phase==='unreachable'){pill.className='pill failed';pill.textContent='Needs Work';evidence.textContent=`Known-name topology receipt: no Pi3 target reached TCP/22 across ${a.length||0} bounded names. Newer physical evidence is not inferred from that older result.`}
 else if(state.phase==='armed'){pill.className='pill experiment';pill.textContent='Advancing';evidence.textContent='Read-only Pi3 physical-evidence lane is implemented and requested; no terminal topology or real Pi3 identity receipt is current yet.'}
 else{pill.className='pill unknown';pill.textContent='Unknown';evidence.textContent=state.error?`Pi3 physical evidence is incomplete: ${state.error} · no human action inferred.`:'Pi3 physical evidence is incomplete · no human action inferred.'}
 const receiptAge=captured?age((attachedReceiptCaptured()?state.attachedReceipt?.observed_at_utc:state.receipt?.observed_at_utc)||rr.captured_at):(attachedUnreachable||identityBoundary)?age(state.attachedReceipt?.observed_at_utc):unreachable?age(state.receipt?.observed_at_utc):null;
 let need;if(gen2){const kr=state.kernelPreflightRun;if(kr&&active(kr))need='Let the bounded kernel-canary prerequisite preflight reach a terminal result. It may probe exact headers and compile an inert module in /tmp, but it must not load a module, change driver binding, change firmware, or grant kernel mutation authority.';else if(kr?.status==='completed'&&kr?.conclusion==='success')need='Persist and inspect the kernel preflight result. Even a green compile-only preflight does not prove an automatic out-of-band watchdog or grant explicit kernel mutation authority; keep kernel load/binding changes held until both later gates are separately proven.';else if(kr&&failed(kr))need='Diagnose the failed compile-only kernel preflight without weakening strict SSH identity, rollback, watchdog, or authority gates. Generation-2 userspace proof remains valid and separate.';else need='Run or finish only the already-prepared safe kernel prerequisite preflight when runner capacity permits; kernel mutation remains held until matching build prerequisites, automatic out-of-band recovery, and fresh explicit authority are all separately proven.'}
 else if(captured)need='Feed the captured receipt through the observe-only StateWeave + Adaptive Kernel/Future Branch trial and collect resulting experiment evidence. Physical promotion, kernel replacement, firmware/network changes, and LKG mutation remain separate later proof gates.';
 else if(identityBoundary)need=`Keep the strict identity boundary intact on ${ATTACHED_RUNNER}. Diagnose why the passive USB TCP/22 endpoint cannot satisfy the existing trusted alias/host-key/authentication path; do not add trust automatically or weaken BatchMode/host-key verification.`;
 else if(attachedUnreachable)need=`Diagnose passive USB adapter/neighbor visibility and TCP/22 reachability on ${ATTACHED_RUNNER}. The receipt does not prove power loss, cable failure, or that a person must intervene.`;
 else if(state.phase==='runner-routing'||state.phase==='runner-routing-armed')need=`Let the bounded attached-runner race reach ${ATTACHED_RUNNER} and publish a terminal receipt. Wrong-runner jobs are allowed to exit without probing; only the workstation previously associated with the Pi3 USB endpoint may continue.`;
 else if(unreachable)need=`The earlier bounded names (${attemptText()}) produced no TCP/22 route. Continue machine-side evidence paths before escalating; do not infer that the Pi3 is powered off, physically disconnected, mislabeled, or in need of human intervention.`;
 else if(state.phase==='attention')need='Diagnose the failed read-only probe job or transport and obtain a fresh terminal topology result or real Pi3 identity receipt. Do not infer a human-only fault until evidence proves it.';
 else need='Let the read-only self-hosted probe finish and publish a fresh terminal topology result or real Pi3 identity receipt. Diagnose topology/SSH reachability as system work before asking for physical intervention.';
 const gen2Model=gen2?g.target?.model:null;const model=gen2Model||rr.model||'not proven';const arch=gen2?'userspace physical proof; architecture held by current Pi3 state':rr.arch||'not proven';
 if(detail)detail.innerHTML=`<b>Pi3 physical evidence boundary</b><div class="p3-grid"><b>Generation-2 proof</b><span>${gen2?`passed physical userspace · ${esc(g.observed_at_utc||'time unknown')} · run ${esc(g.workflow_run_id||'unknown')}`:'not verified'}</span><b>Adaptive candidate</b><span>${gen2?`${esc(g.generation2?.candidate_id)} · score ${esc(g.generation2?.score)} · read evidence complete`:'not verified'}</span><b>Fault quarantine</b><span>${gen2?`${esc(g.fault_injection?.decision)} · ${esc(g.fault_injection?.reason)} · missing ${esc((g.fault_injection?.missing_fields||[]).join(', ')||'field')}`:'not verified'}</span><b>LKG preservation</b><span>${gen2&&g.fault_injection?.lkg_sha256_before===g.fault_injection?.lkg_sha256_after?'byte-identical metadata SHA before/after fault':'not verified'}</span><b>Isolated rollback</b><span>${gen2&&g.isolated_metadata_rollback?.passed?`passed → ${esc(g.isolated_metadata_rollback?.restored_profile_id)}`:'not verified'}</span><b>System driver changed</b><span>false</span><b>Kernel module loaded</b><span>false</span><b>Kernel binding changed</b><span>false</span><b>Firmware mutation</b><span>false</span><b>Kernel mutation authority</b><span>false</span><b>Kernel preflight request</b><span>${kernelPreflightRequestSafe()?'safe compile-only / read-only':'not safely proven'}</span><b>Kernel preflight workflow</b><span>${kernelPreflightWorkflowGuarded()?esc(kernelPreflightText()):'guard contract not fully proven'}</span><b>Known-name request</b><span>${safeRequest()?`safe read-only · ${esc(state.request?.request_id||'request id unknown')}`:'not safely proven'}</span><b>Attached-runner request</b><span>${attachedRequestSafe()?`safe read-only · target ${ATTACHED_RUNNER}`:'not active / not safely proven'}</span><b>Active network scan</b><span>false</span><b>Persistent SSH trust change</b><span>false</span><b>Write authority</b><span>false</span><b>Promotion authority</b><span>false</span><b>Model</b><span>${esc(model)}</span><b>Architecture</b><span>${esc(arch)}</span><b>Adaptive Kernel gate</b><span>${esc(kernelGateText())}</span><b>Legacy topology evidence</b><span>${unreachable?`older no-reachable receipt · ${esc(ageText(age(state.receipt?.observed_at_utc)))} old · ${esc(attemptText())}`:captured?'identity receipt captured':'not terminal / superseded by stronger proof when present'}</span><b>Root cause</b><span>not inferred</span></div><br><b>Frontiers Advancing:</b> ${gen2?`physical userspace Generation 2 is now proven on ${esc(g.target?.model||'the pinned Pi3')}: ${esc(g.generation2?.candidate_id)} scored 100, a missing-carrier fault was quarantined, LKG metadata remained byte-identical, and isolated rollback restored ${esc(g.isolated_metadata_rollback?.restored_profile_id||'the reference profile')}. This is real-hardware adaptive-driver evidence, not repository-only activity.`:'the Pi3 lane preserves read-only physical discovery, strict identity, StateWeave binding, and architecture-gated experimentation without broadening authority.'} ${kernelPreflightRequestSafe()&&kernelPreflightWorkflowGuarded()?`A separate kernel-canary prerequisite lane is safely prepared as compile-only/read-only evidence; current run state: ${esc(kernelPreflightText())}.`:''}<br><b>Needs Work → Aurum/System:</b> ${esc(need)} Physical kernel-module mutation, driver-binding mutation, firmware mutation, production ARM64 Tiny Seed proof, and Guardian physical kernel rollback remain unproven and must not be inferred from userspace success.<br><b>Your Actions:</b> none for this Pi3 lane. Physical userspace success, a queued/green/failed compile-only preflight, topology evidence, runner state, and prediction cannot manufacture kernel authority or a human task. Exact physical directions belong here only after fresh evidence proves a genuinely human-only boundary.`;
 window.__aurumPi3PhysicalEvidenceState={schema:'aurum-command-center-pi3-physical-evidence-v1.4',componentRevision:'1.4',phase:state.phase,canonicalOwner:'FormatX66/ClusterSites',requestSafeReadOnly:safeRequest(),probeImplementationPresent:codeGatePresent(),probeWorkflowGuarded:workflowGuarded(),parallelExperimentGatePresent:experimentGatePresent(),attachedRunnerRequestSafeReadOnly:attachedRequestSafe(),attachedRunnerWorkflowGuarded:attachedWorkflowGuarded(),attachedRunnerRouteTarget:ATTACHED_RUNNER,attachedRunnerReceiptState:state.attachedReceipt?.state||'not-verified',attachedRunnerReceiptCaptured:attachedReceiptCaptured(),attachedRunnerIdentityUnverified:attachedIdentityBoundary(),attachedRunnerNoPassiveEndpoint:attachedNoEndpoint(),activeNetworkScanAllowed:false,persistentTrustChangeAllowed:false,physicalReceiptCaptured:captured||gen2,generation2PhysicalUserspaceProof:gen2,generation2CandidateId:gen2?g.generation2?.candidate_id||null:null,generation2Score:gen2?Number(g.generation2?.score):null,generation2FaultInjectionQuarantined:gen2&&g.fault_injection?.decision==='quarantined',generation2LkgMetadataPreserved:gen2&&g.fault_injection?.lkg_sha256_before===g.fault_injection?.lkg_sha256_after,generation2MetadataRollbackPassed:gen2&&g.isolated_metadata_rollback?.passed===true,kernelCanaryPreflightRequestSafe:kernelPreflightRequestSafe(),kernelCanaryPreflightWorkflowGuarded:kernelPreflightWorkflowGuarded(),kernelCanaryPreflightStatus:state.kernelPreflightRun?.conclusion||state.kernelPreflightRun?.status||'not-verified',kernelModuleLoaded:false,kernelDriverMutationAuthority:false,noReachableReceiptObserved:unreachable,reachabilityReceiptState:state.receipt?.state||'not-verified',knownTargetsTried:a.length,tcp22ReachableCount:a.filter(x=>x?.tcp22_reachable===true).length,rootCauseInferred:false,physicalReceiptModel:gen2?g.target?.model||null:captured?rr.model||null:null,adaptiveKernelGate:kernelGateText(),stateWeaveHardwareBinding:codeGatePresent(),mutationAllowed:false,promotionAllowed:false,writeAuthority:false,needsWorkOwner:'aurum-system',humanActionInference:false};window.dispatchEvent(new CustomEvent('aurum-pi3-physical-evidence-state',{detail:window.__aurumPi3PhysicalEvidenceState}));}
async function refresh(){state={...state,phase:'checking',error:''};render();try{const [request,receipt,probeText,probeWorkflowText,expWorkflowText,attachedRequest,attachedReceipt,attachedWorkflowText,gen2,kernelPreflightRequest,kernelPreflightWorkflowText,runs]=await Promise.all([getJson(REQUEST_URL),getJson(RECEIPT_URL,true),getText(PROBE_URL),getText(PROBE_WORKFLOW_URL),getText(EXP_WORKFLOW_URL),getJson(ATTACHED_REQUEST_URL,true),getJson(ATTACHED_RECEIPT_URL,true),getText(ATTACHED_WORKFLOW_URL,true),getJson(GEN2_URL,true),getJson(KERNEL_PREFLIGHT_REQUEST_URL,true),getText(KERNEL_PREFLIGHT_WORKFLOW_URL,true),getJson(`${API}/actions/runs?per_page=100`)]);const probeRun=latestRun(runs?.workflow_runs,PROBE_WORKFLOW),attachedRun=latestRun(runs?.workflow_runs,ATTACHED_WORKFLOW),expRun=latestRun(runs?.workflow_runs,EXP_WORKFLOW),kernelPreflightRun=latestRun(runs?.workflow_runs,KERNEL_PREFLIGHT_WORKFLOW);state={phase:'checking',request,receipt,probeText,probeWorkflowText,expWorkflowText,attachedRequest,attachedReceipt,attachedWorkflowText,gen2,kernelPreflightRequest,kernelPreflightWorkflowText,probeRun,probeRunAge:probeRun?age(probeRun.updated_at||probeRun.created_at):null,attachedRun,attachedRunAge:attachedRun?age(attachedRun.updated_at||attachedRun.created_at):null,expRun,expRunAge:expRun?age(expRun.updated_at||expRun.created_at):null,kernelPreflightRun,kernelPreflightRunAge:kernelPreflightRun?age(kernelPreflightRun.updated_at||kernelPreflightRun.created_at):null,error:''}}catch(e){state={...state,error:e?.message||'request failed'}}render()}
function boot(){if(!$('#systems')){setTimeout(boot,250);return}ensure();refresh();setInterval(refresh,REFRESH)}boot();
})();