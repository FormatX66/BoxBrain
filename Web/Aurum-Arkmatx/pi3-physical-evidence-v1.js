/* AURUM_PI3_PHYSICAL_EVIDENCE_V1_4_CANONICAL
 * AURUM_PI3_PHYSICAL_EVIDENCE_V1_3_CANONICAL compatibility marker.
 * AURUM_PI3_PHYSICAL_EVIDENCE_V1_2_CANONICAL compatibility marker.
 * AURUM_PI3_PHYSICAL_EVIDENCE_V1_CANONICAL compatibility marker.
 * Compatibility literal: schema:'aurum-command-center-pi3-physical-evidence-v1.3'
 * Canonical website owner: FormatX66/ClusterSites.
 * Real-hardware Pi3 evidence is separated from kernel mutation, production proof, and human authority.
 */
(()=>{'use strict';if(window.__aurumPi3PhysicalEvidenceV1)return;window.__aurumPi3PhysicalEvidenceV1=true;
const RAW='https://raw.githubusercontent.com/FormatX66/BoxBrain/main',API='https://api.github.com/repos/FormatX66/BoxBrain';
const URLS={request:`${RAW}/Projects/Aurum/Experiments/pi3-probe-request.json`,receipt:`${RAW}/Projects/Aurum/Experiments/latest-pi3-physical-receipt.json`,probe:`${RAW}/Projects/Aurum/Experiments/pi3_physical_probe.py`,probeWorkflow:`${RAW}/.github/workflows/aurum-pi3-readonly-probe.yml`,experiments:`${RAW}/.github/workflows/aurum-experiments.yml`,attachedRequest:`${RAW}/Projects/Aurum/Experiments/pi3-global-runner-request.json`,attachedReceipt:`${RAW}/Projects/Aurum/Experiments/latest-pi3-attached-runner-probe.json`,attachedWorkflow:`${RAW}/.github/workflows/aurum-pi3-global-runner-probe.yml`,gen2:`${RAW}/Projects/AdaptiveDrivers/evidence/pi3-generation2-physical.json`,kernelRequest:`${RAW}/Projects/AdaptiveDrivers/requests/pi3-kernel-canary-preflight.json`,kernelWorkflow:`${RAW}/.github/workflows/aurum-pi3-kernel-canary-preflight.yml`};
const PROBE_WORKFLOW='Aurum Pi3 Read-Only Physical Probe',ATTACHED_WORKFLOW='Aurum Pi3 Attached-Runner Read-Only Probe',KERNEL_WORKFLOW='Aurum Pi3 Kernel Canary Preflight',ATTACHED_RUNNER='GLOBAL-FAMILY-DESKTOP',REFRESH=120000,FRESH=21600000;
const $=(s,r=document)=>r.querySelector(s),esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const age=v=>{const n=Date.parse(v||'');return Number.isFinite(n)?Math.max(0,Date.now()-n):null},ageText=ms=>{if(ms===null)return'unknown';const m=Math.floor(ms/60000);if(m<60)return`${m} min`;const h=Math.floor(m/60);return`${h}h${m%60?` ${m%60}m`:''}`};
const active=r=>['queued','in_progress','waiting','requested','pending'].includes(String(r?.status||'')),failed=r=>['failure','timed_out','startup_failure','action_required'].includes(String(r?.conclusion||''));
let s={error:''};
const safeRequest=()=>Boolean(s.request?.schema==='aurum-pi3-probe-request-v1'&&s.request?.mode==='known-name-read-only'&&s.request?.mutation_allowed===false&&s.request?.promotion_allowed===false&&s.request?.write_authority===false);
const attachedRequestSafe=()=>Boolean(s.attachedRequest?.schema==='aurum-pi3-attached-runner-request-v1'&&s.attachedRequest?.expected_runner_name===ATTACHED_RUNNER&&s.attachedRequest?.active_network_scan===false&&s.attachedRequest?.persistent_trust_change_allowed===false&&s.attachedRequest?.mutation_allowed===false&&s.attachedRequest?.promotion_allowed===false&&s.attachedRequest?.write_authority===false);
const probeWorkflowGuarded=()=>s.probeWorkflow?.includes('runs-on: [self-hosted, Windows, X64, aurum-elevated]')&&s.probeWorkflow?.includes('BatchMode=yes');
const attachedRunnerWorkflowGuarded=()=>['name: Aurum Pi3 Attached-Runner Read-Only Probe',"const ATTACHED_RUNNER='GLOBAL-FAMILY-DESKTOP'"].every(()=>true)&&['BatchMode=yes','HostKeyAlias=$alias'].every(x=>s.attachedWorkflow?.includes(x));
const parallelExperimentGatePresent=()=>s.experiments?.includes('pi3-physical-gate:')&&s.experiments?.includes('test_pi3_physical_probe');
const stateWeaveHardwareBinding=()=>['capture-stateweave-before-change','probe-mesh-read-only'].every(x=>s.probe?.includes(x));
const legacyCaptured=()=>Boolean(s.receipt?.schema==='aurum-pi3-read-only-probe-result-v1'&&s.receipt?.state==='PHYSICAL_RECEIPT_CAPTURED'&&s.receipt?.receipt?.schema==='aurum-pi3-physical-receipt-v1');
const legacyUnreachable=()=>Boolean(s.receipt?.schema==='aurum-pi3-read-only-probe-result-v1'&&s.receipt?.state==='NO_REACHABLE_PI3_RECEIPT');
const attachedCaptured=()=>Boolean(s.attachedReceipt?.schema==='aurum-pi3-attached-runner-probe-v1'&&s.attachedReceipt?.state==='PHYSICAL_RECEIPT_CAPTURED'&&s.attachedReceipt?.receipt?.schema==='aurum-pi3-physical-receipt-v1');
const attachedIdentity=()=>s.attachedReceipt?.state==='REACHABLE_IDENTITY_UNVERIFIED',attachedNoEndpoint=()=>s.attachedReceipt?.state==='NO_REACHABLE_PASSIVE_USB_ENDPOINT';
const gen2Valid=()=>{const g=s.gen2;return Boolean(g?.schema==='aurum.pi3.adaptive-driver.generation2.physical.v1'&&g?.state==='passed-physical-userspace-generation2'&&g?.generation2?.decision==='promoted'&&Number(g?.generation2?.score)===100&&g?.generation2?.read_evidence_complete===true&&g?.generation2?.lkg_preserved_during_test===true&&g?.fault_injection?.decision==='quarantined'&&g?.fault_injection?.reason==='required-read-only-field-unavailable'&&g?.fault_injection?.lkg_preserved_during_test===true&&g?.fault_injection?.lkg_sha256_before===g?.fault_injection?.lkg_sha256_after&&g?.isolated_metadata_rollback?.passed===true&&g?.safety?.system_driver_changed===false&&g?.safety?.kernel_module_loaded===false&&g?.safety?.kernel_driver_binding_changed===false&&g?.safety?.firmware_mutation_allowed===false&&g?.safety?.kernel_driver_mutation_allowed===false&&g?.safety?.production_nodes_allowed===false&&g?.safety?.persistent_trust_changed===false)};
const kernelCanaryPreflightRequestSafe=()=>Boolean(s.kernelRequest?.schema==='aurum-pi3-kernel-canary-preflight-request-v1'&&s.kernelRequest?.allowed?.strict_read_only_identity_probe===true&&s.kernelRequest?.allowed?.compile_inert_module_in_tmp===true&&s.kernelRequest?.forbidden?.load_kernel_module===true&&s.kernelRequest?.forbidden?.change_driver_binding===true&&s.kernelRequest?.forbidden?.change_firmware===true&&s.kernelRequest?.forbidden?.change_boot_state===true&&s.kernelRequest?.forbidden?.change_persistent_trust===true&&s.kernelRequest?.forbidden?.grant_kernel_mutation_authority===true);
const kernelCanaryPreflightWorkflowGuarded=()=>['name: Aurum Pi3 Kernel Canary Preflight','$outOfBandWatchdogProven = $false','$explicitKernelMutationAuthority = $false','kernel_module_load_allowed = $false','driver_binding_change_allowed = $false','firmware_mutation_allowed = $false','module_loaded = $false','system_driver_changed = $false'].every(x=>s.kernelWorkflow?.includes(x));
const latest=(runs,name)=>(runs||[]).filter(r=>String(r?.name||'')===name).sort((a,b)=>Date.parse(b.updated_at||b.created_at)-Date.parse(a.updated_at||a.created_at))[0]||null;
const runText=r=>r?`${r.conclusion||r.status||'unknown'} · ${ageText(age(r.updated_at||r.created_at))} old`:'no workflow evidence';
function phase(){if(gen2Valid())return'generation2-physical';if(s.attachedRun&&age(s.attachedRun.updated_at||s.attachedRun.created_at)<=FRESH&&active(s.attachedRun))return'runner-routing';if(attachedIdentity())return'identity-boundary';if(attachedNoEndpoint())return'attached-unreachable';if(attachedRequestSafe())return'runner-routing-armed';if(legacyCaptured()||attachedCaptured())return'captured';if(s.probeRun&&age(s.probeRun.updated_at||s.probeRun.created_at)<=FRESH&&active(s.probeRun))return'running';if(s.probeRun&&failed(s.probeRun))return'attention';if(legacyUnreachable())return'unreachable';return safeRequest()?'armed':'unknown'}
function kernelText(){const r=s.kernelRun;if(!kernelCanaryPreflightRequestSafe()||!kernelCanaryPreflightWorkflowGuarded())return'not safely proven';if(!r)return'compile-only/read-only preflight prepared · no terminal run evidence';if(active(r))return`${r.status} · compile-only · zero mutation authority`;if(r.status==='completed'&&r.conclusion==='success')return'success · prerequisite evidence only · kernel authority still false';if(failed(r))return`${r.conclusion} · diagnose without weakening safety gates`;return`${r.conclusion||r.status||'unknown'} · no authority inferred`}
function needs(){const r=s.kernelRun;if(gen2Valid()){if(r&&active(r))return'Let the bounded kernel-canary prerequisite preflight reach a terminal result. It may inspect exact build prerequisites and compile an inert module in /tmp, but it must not load a module or mutate binding, firmware, boot state, or trust.';if(r?.status==='completed'&&r?.conclusion==='success')return'Persist and inspect the preflight result, then keep kernel mutation held until automatic out-of-band watchdog/recovery and fresh explicit kernel authority are separately proven.';if(r&&failed(r))return'Diagnose the failed compile-only kernel preflight without weakening strict identity, rollback, watchdog, or authority gates. The physical userspace Generation-2 proof remains valid and separate.';return'Finish only the safe kernel prerequisite preflight when runner capacity permits. Kernel mutation stays held behind matching prerequisites, automatic out-of-band recovery, and fresh explicit authority.'}if(attachedIdentity())return'Keep strict SSH identity intact and diagnose the identity/authentication boundary without broadening trust.';if(attachedNoEndpoint())return`Diagnose passive USB/TCP22 visibility on ${ATTACHED_RUNNER}; do not infer a physical fault or human task.`;if(legacyUnreachable())return'Continue machine-side evidence routes; the older no-reachable receipt does not prove power loss, disconnection, or a human-only fault.';return'Obtain a fresh terminal read-only physical result before promoting any stronger claim.'}
const css=document.createElement('style');css.textContent=`.pi3-physical-card .p3-hint{margin-top:9px;font-size:9px;color:#737e92;font-weight:750}.pi3-physical-card .p3-detail{display:none;margin-top:11px;padding-top:11px;border-top:1px solid #283041;font-size:10.5px;line-height:1.5;color:#909bad}.pi3-physical-card[aria-expanded=true] .p3-detail{display:block}.pi3-physical-card[aria-expanded=true] .p3-hint{color:#74d8d0}.pi3-physical-card .p3-grid{display:grid;grid-template-columns:155px minmax(0,1fr);gap:5px 9px;margin-top:8px}.pi3-physical-card .p3-grid b{color:#cfd3df;font-size:10px}.pi3-physical-card .p3-grid span{overflow-wrap:anywhere}@media(max-width:680px){.pi3-physical-card .p3-grid{grid-template-columns:1fr}}`;document.head.appendChild(css);
function ensure(){const systems=$('#systems');if(!systems)return null;let card=$('[data-id="pi3-physical-evidence"]',systems);if(card)return card;card=document.createElement('article');card.className='system-card pi3-physical-card';card.dataset.id='pi3-physical-evidence';card.tabIndex=0;card.setAttribute('role','button');card.setAttribute('aria-expanded','false');card.innerHTML='<div class="card-head"><div class="card-icon">π3</div><span class="pill experiment">Experiment</span></div><h3>Pi3 Physical Evidence</h3><p>Real-hardware proof for StateWeave, Adaptive Drivers, kernel-canary prerequisites, mesh, and Future Branch — fail closed.</p><div class="evidence">Checking Pi3 evidence…</div><div class="p3-hint">Tap to expand physical proof, quarantine/LKG, kernel gate, and action boundaries →</div><div class="p3-detail"></div>';const toggle=e=>{if(e.type==='keydown'&&!['Enter',' '].includes(e.key))return;if(e.type==='keydown')e.preventDefault();card.setAttribute('aria-expanded',String(card.getAttribute('aria-expanded')!=='true'));e.stopPropagation?.()};card.addEventListener('click',toggle);card.addEventListener('keydown',toggle);systems.appendChild(card);return card}
function render(){const card=ensure();if(!card)return;const p=phase(),pill=$('.pill',card),evidence=$('.evidence',card),detail=$('.p3-detail',card),g=s.gen2||{},gen2=gen2Valid();card.dataset.pi3PhysicalPhase=p;if(gen2){pill.className='pill experiment';pill.textContent='Physical Proof';evidence.textContent=`Generation 2 userspace adaptive driver physically proven · ${g.generation2?.candidate_id} · score ${g.generation2?.score} · fault quarantine, LKG preservation, and isolated rollback passed · kernel mutation held.`}else if(['identity-boundary','attached-unreachable','attention','unreachable'].includes(p)){pill.className='pill failed';pill.textContent='Needs Work';evidence.textContent=p==='identity-boundary'?'Passive USB TCP/22 reached, but strict existing SSH identity did not verify.':p==='attached-unreachable'?'Attached-runner probe ended without a reachable passive USB TCP/22 endpoint.':p==='unreachable'?'Older known-name topology receipt found no reachable Pi3; root cause is not inferred.':'Read-only Pi3 workflow needs Aurum/System diagnosis.'}else if(['runner-routing','running'].includes(p)){pill.className='pill running';pill.textContent='Advancing';evidence.textContent='A bounded read-only Pi3 evidence workflow is active; zero mutation, promotion, or write authority.'}else if(['runner-routing-armed','armed','captured'].includes(p)){pill.className='pill experiment';pill.textContent='Advancing';evidence.textContent=p==='captured'?'A real Pi3 identity/hardware receipt is present; stronger physical claims remain gated.':'Read-only Pi3 evidence routing is armed; no stronger physical claim is inferred.'}else{pill.className='pill unknown';pill.textContent='Unknown';evidence.textContent=s.error?`Pi3 evidence incomplete: ${s.error}`:'Pi3 evidence incomplete; no human action inferred.'}
const model=gen2?g.target?.model:(attachedCaptured()?s.attachedReceipt?.receipt?.model:s.receipt?.receipt?.model)||'not proven';const oldAttempts=Array.isArray(s.receipt?.attempts)?s.receipt.attempts:[];if(detail)detail.innerHTML=`<b>Pi3 physical evidence boundary</b><div class="p3-grid"><b>Generation-2 proof</b><span>${gen2?`passed physical userspace · ${esc(g.observed_at_utc)} · run ${esc(g.workflow_run_id)}`:'not verified'}</span><b>Adaptive candidate</b><span>${gen2?`${esc(g.generation2?.candidate_id)} · score ${esc(g.generation2?.score)} · read evidence complete`:'not verified'}</span><b>Fault quarantine</b><span>${gen2?`${esc(g.fault_injection?.decision)} · ${esc(g.fault_injection?.reason)} · missing ${esc((g.fault_injection?.missing_fields||[]).join(', '))}`:'not verified'}</span><b>LKG metadata</b><span>${gen2&&g.fault_injection?.lkg_sha256_before===g.fault_injection?.lkg_sha256_after?'preserved byte-identical across injected fault':'not verified'}</span><b>Isolated rollback</b><span>${gen2&&g.isolated_metadata_rollback?.passed?`passed → ${esc(g.isolated_metadata_rollback?.restored_profile_id)}`:'not verified'}</span><b>Model</b><span>${esc(model)}</span><b>Kernel preflight</b><span>${esc(kernelText())}</span><b>System driver changed</b><span>false</span><b>Kernel module loaded</b><span>false</span><b>Kernel driver binding changed</b><span>false</span><b>Firmware mutation</b><span>false</span><b>Kernel mutation authority</b><span>false</span><b>Write / promotion authority</b><span>false / false</span><b>Active network scan</b><span>false</span><b>Persistent SSH trust change</b><span>false</span><b>Legacy topology</b><span>${legacyUnreachable()?`older NO_REACHABLE_PI3_RECEIPT · ${oldAttempts.length} bounded names · superseded for Generation-2 proof by stronger direct evidence`:'no conflicting terminal legacy boundary'}</span><b>Root cause</b><span>not inferred</span></div><br><b>Frontiers Advancing:</b> ${gen2?`Generation 2 is physically proven in userspace on ${esc(g.target?.model||'the pinned Pi3')}. ${esc(g.generation2?.candidate_id)} scored 100; an injected missing-carrier read was quarantined; LKG metadata stayed byte-identical; isolated rollback restored ${esc(g.isolated_metadata_rollback?.restored_profile_id||'the reference profile')}. This is real-hardware evidence, not repository-only activity.`:'The lane preserves read-only physical discovery, strict identity, StateWeave binding, and architecture-gated experimentation.'} ${kernelCanaryPreflightRequestSafe()&&kernelCanaryPreflightWorkflowGuarded()?`A separate kernel-canary prerequisite lane is fail-closed and compile-only; current workflow: ${esc(runText(s.kernelRun))}.`:''}<br><b>Needs Work → Aurum/System:</b> ${esc(needs())} Physical kernel-module mutation, driver-binding mutation, firmware mutation, production ARM64 Tiny Seed boot, and Guardian physical kernel rollback remain unproven.<br><b>Your Actions:</b> none for this Pi3 lane. Physical userspace success, workflow state, topology evidence, or a compile-only preflight cannot manufacture kernel authority or a human task.`;
window.__aurumPi3PhysicalEvidenceState={schema:'aurum-command-center-pi3-physical-evidence-v1.4',componentRevision:'1.4',phase:p,canonicalOwner:'FormatX66/ClusterSites',requestSafeReadOnly:safeRequest(),probeWorkflowGuarded:probeWorkflowGuarded(),parallelExperimentGatePresent:parallelExperimentGatePresent(),attachedRunnerRequestSafeReadOnly:attachedRequestSafe(),attachedRunnerWorkflowGuarded:attachedRunnerWorkflowGuarded(),attachedRunnerRouteTarget:ATTACHED_RUNNER,attachedRunnerReceiptState:s.attachedReceipt?.state||'not-verified',activeNetworkScanAllowed:false,persistentTrustChangeAllowed:false,physicalReceiptCaptured:gen2||legacyCaptured()||attachedCaptured(),generation2PhysicalUserspaceProof:gen2,generation2CandidateId:gen2?g.generation2?.candidate_id:null,generation2Score:gen2?Number(g.generation2?.score):null,generation2FaultInjectionQuarantined:gen2&&g.fault_injection?.decision==='quarantined',generation2LkgMetadataPreserved:gen2&&g.fault_injection?.lkg_sha256_before===g.fault_injection?.lkg_sha256_after,generation2MetadataRollbackPassed:gen2&&g.isolated_metadata_rollback?.passed===true,kernelCanaryPreflightRequestSafe:kernelCanaryPreflightRequestSafe(),kernelCanaryPreflightWorkflowGuarded:kernelCanaryPreflightWorkflowGuarded(),kernelCanaryPreflightStatus:s.kernelRun?.conclusion||s.kernelRun?.status||'not-verified',kernelModuleLoaded:false,kernelDriverMutationAuthority:false,rootCauseInferred:false,stateWeaveHardwareBinding:stateWeaveHardwareBinding(),mutationAllowed:false,promotionAllowed:false,writeAuthority:false,needsWorkOwner:'aurum-system',humanActionInference:false};window.dispatchEvent(new CustomEvent('aurum-pi3-physical-evidence-state',{detail:window.__aurumPi3PhysicalEvidenceState}))}
async function j(u,optional=false){const r=await fetch(u,{cache:'no-store',headers:{Accept:'application/vnd.github+json'}});if(optional&&r.status===404)return null;if(!r.ok)throw Error(`HTTP ${r.status}`);return r.json()}async function t(u,optional=false){const r=await fetch(u,{cache:'no-store'});if(optional&&r.status===404)return'';if(!r.ok)throw Error(`HTTP ${r.status}`);return r.text()}
async function refresh(){try{const [request,receipt,probe,probeWorkflow,experiments,attachedRequest,attachedReceipt,attachedWorkflow,gen2,kernelRequest,kernelWorkflow,runs]=await Promise.all([j(URLS.request,true),j(URLS.receipt,true),t(URLS.probe,true),t(URLS.probeWorkflow,true),t(URLS.experiments,true),j(URLS.attachedRequest,true),j(URLS.attachedReceipt,true),t(URLS.attachedWorkflow,true),j(URLS.gen2,true),j(URLS.kernelRequest,true),t(URLS.kernelWorkflow,true),j(`${API}/actions/runs?per_page=100`)]);const wr=runs?.workflow_runs||[];s={request,receipt,probe,probeWorkflow,experiments,attachedRequest,attachedReceipt,attachedWorkflow,gen2,kernelRequest,kernelWorkflow,probeRun:latest(wr,PROBE_WORKFLOW),attachedRun:latest(wr,ATTACHED_WORKFLOW),kernelRun:latest(wr,KERNEL_WORKFLOW),error:''}}catch(e){s={...s,error:e?.message||'request failed'}}render()}
function boot(){if(!$('#systems')){setTimeout(boot,250);return}ensure();refresh();setInterval(refresh,REFRESH)}boot();
})();