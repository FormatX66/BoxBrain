# Ideas

- Show the Ethernet peer MAC and authorization state as soon as neighbor
  discovery observes it.
- Add a read-only deployment version mismatch banner when the Pi API is newer
  than the local controller/UI schema.
