# Questions

1. Is Pi `eth0` connected to this Windows workstation or a different target?
2. Which Windows Ethernet adapter should own the direct-link address?
3. Should the local controller/dashboard rollout happen before target enrollment?
