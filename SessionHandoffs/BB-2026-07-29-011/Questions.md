# Questions

1. Should pointer scrolling be the next native capability, as recommended, or
   should shell or clipboard come first?
2. Should FreeRDP 3.15 builds plus the exact Pi 3.26 on-host gate remain the
   compatibility contract for the next run?
3. Should the verified Notepad click-and-type workflow become a fixed
   automated acceptance scenario?
4. Which BoxBrain project should follow BrainConnect in priority?
5. What is Arkmatx's purpose and where are its current files or repository?
6. Should the retired checkpoint remain indefinitely, or should a separate
   maintenance window be planned to review its deletion and disk merge?
