# Questions

1. Which Windows users own sessions 1 and the LogonUI sessions created by the
   FreeRDP attempts?
2. Is `fSingleSessionPerUser` enabled, and do Terminal Services logs show a
   reconnect, a fresh session, or an interactive-logon failure?
3. Should BrainConnect explicitly request an administrative/console session,
   or should the lab guarantee one reconnectable session for `boxbrain-lab`?
4. What bounded evidence should verify typed text after Notepad process
   presence is proven?
5. Should session identity become a required field in adapter results or a
   separate verifier record?
