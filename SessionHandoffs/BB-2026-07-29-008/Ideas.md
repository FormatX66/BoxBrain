# Ideas

- Add a read-only Hyper-V PowerShell Direct report for RDP session IDs, users,
  states, lock state, and recent Terminal Services events.
- Add a lab-only application that exposes a non-sensitive input marker so text
  content can be verified without retaining a full screenshot.
- Record `transport_accepted`, `session_matched`, and `state_verified` as three
  distinct benchmark outcomes.
- Refuse live input when the intended session cannot be identified before the
  executor window opens.
- After the next sign-in activates Hyper-V group membership, replace repeated
  UAC prompts with the same guarded non-elevated restore helper.
