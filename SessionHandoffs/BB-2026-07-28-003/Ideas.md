# Ideas

- Add a certificate-probe screen that displays subject, issuer, endpoint, and
  fingerprint without accepting or authenticating to the target.
- Require a second typed fingerprint fragment when enabling a target.
- Add an expiring approval state for temporary lab targets.
- Provide an evidence-retention preview before enabling observation.
- Validate FreeRDP launch arguments against a fixed allowlist in the plugin
  process rather than accepting arbitrary command-line options.
- Add a repository rule that runs BoxBrain structural validation on changes to
  Admin, Architecture, Projects, or SessionHandoffs.
