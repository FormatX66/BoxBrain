# Questions

1. Which dedicated low-privilege Windows account should own the first RDP
   session and credential set?
2. Should the credential drop-in load only this one target UUID or use a
   separately managed directory for future targets?
3. What independent evidence can prove cursor movement inside the authenticated
   RDP session before frame observation exists?
4. Should the existing native installer become a two-artifact selector or
   install both reviewed helpers atomically?
5. Does the Pi's systemd version support the intended encrypted credential
   workflow without adding a package?
6. Should a restored checkpoint automatically trigger certificate re-probe
   before any new credentialed session?
