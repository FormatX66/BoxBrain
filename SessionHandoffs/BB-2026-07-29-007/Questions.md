# Questions

1. Which authorized Hyper-V account or service should own automated checkpoint
   restoration?
2. Should the next verifier use bounded RDP frames, a guest-side test marker,
   or both?
3. Should a sequence be represented as one operation with several input events
   or as several operations bound to one short-lived session token?
4. What minimum evidence is sufficient to prove pointer movement without
   retaining a full screenshot?
5. Should a verified state-change field be added to adapter protocol version 2
   or remain a separate verifier result?
6. After visual verification, should pointer button input or scrolling be the
   next native capability?
