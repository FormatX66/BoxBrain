# Ideas

- Make the planner produce only versioned structured actions already accepted
  by the controller.
- Add per-task action, time, token, and cost budgets before calling a model.
- Store benchmark inputs and redacted result summaries without retaining frame
  payloads.
- Promote the fixed scrolling proof into a repeatable release gate.
