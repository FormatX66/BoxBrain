# Execution Plan

## 1. Completed Pi RDP identity live lab

1. [x] Inventory available disposable target options.
2. [x] Reject the development host and stop the unsuitable Windows Sandbox
   listener attempt.
3. [x] Add a protocol-faithful RDP/NLA fixture on Pi loopback.
4. [x] Diagnose and fix FreeRDP's minimum runtime environment.
5. [x] Verify exact match and explicit enablement.
6. [x] Rotate the certificate and verify atomic disablement.
7. [x] Verify bounded unreachable and timeout behavior.
8. [x] Prove no authentication, desktop session, or application data.
9. [x] Make immutable controller upgrades fail-safe.
10. [x] Deploy the exact revision, run regressions, and open draft review 7.

## 2. Prepare a disposable full desktop target

1. Choose a full VM or dedicated machine with explicit operator ownership.
2. Isolate its network, accounts, files, and credentials.
3. Snapshot the clean state.
4. Record endpoint identity and certificate fingerprint independently.
5. Confirm the Pi can reach only the intended target endpoint.
6. Keep authentication and every input or redirection capability unavailable.

## 3. Add observation-only frame transport

1. Specify a versioned process protocol and evidence model.
2. Bound resolution, cadence, payload size, memory, retention, and reconnects.
3. Apply redaction before persistence or dashboard delivery.
4. Add protocol, downgrade, malformed-frame, timeout, and backpressure tests.
5. Require the existing exact certificate gate before frames.
6. Run a controlled live observation and restore the target snapshot.

## Completion gates

- Controller and helper identities continue to match Pi provenance.
- API remains authenticated and USB-only.
- Long-lived controller credentials remain outside Git and Flutter assets.
- Only the explicitly authorized disposable target is contacted.
- Frames remain bounded, redacted, and observation-only.
- No RDP credential, authentication, input, shell, clipboard, file, or device
  redirection enters the milestone.
- All BrainConnect and BoxBrain validation passes.
