# Ideas

- Add a deterministic installer-upgrade test with a disposable local systemd
  fixture or container.
- Make controller wheels reproducible so identical packaged source yields the
  same checksum across deployments.
- Add a short-lived dashboard session exchange that never exposes the
  long-lived Pi token to Flutter web assets.
- Define a frame observer as a separate process protocol with strict byte,
  dimension, cadence, and redaction limits.
- Add an authenticated readiness response for deployed revision and helper
  provenance without exposing credentials.
- Export signed, redacted audit bundles for disposable desktop-lab runs.
