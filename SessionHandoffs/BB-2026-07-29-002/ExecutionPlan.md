# Execution Plan

## 1. Completed disposable Windows target

1. [x] Verify official Windows media and edition metadata.
2. [x] Create guarded Generation 2 Hyper-V VM.
3. [x] Bind the VM only to the Pi USB network.
4. [x] Apply Windows to the exact blank VHD.
5. [x] Verify guest identity, heartbeat, address, and RDP.
6. [x] Fix and deploy single-address Pi onboarding.
7. [x] Provision the restricted Pi-only SSH boundary.
8. [x] Bound slow Windows device inventory.
9. [x] Complete a Pi-originated healthy read-only diagnostic.
10. [x] Gracefully shut down and checkpoint the clean target.

## 2. Next certificate gate

1. Start from checkpoint `clean-linked-2026-07-29`.
2. Reconfirm `10.12.194.9:3389`.
3. Observe the RDP certificate independently.
4. Register the target disabled by default.
5. Probe with the native helper and no credential.
6. Confirm the exact fingerprint and audit record.
7. Enable the target only after the match.

## 3. Observation-only frames

1. Define a versioned frame protocol.
2. Bound resolution, cadence, size, memory, and retries.
3. Apply redaction before delivery or persistence.
4. Retain zero raw frames by default.
5. Keep every input and redirection capability unavailable.
6. Add malformed, timeout, disconnect, mismatch, and backpressure tests.
7. Run one controlled live observation.
8. Restore and verify the clean checkpoint.

## Completion gates

- VM identity and checkpoint match the approved baseline.
- RDP certificate matches an independent record.
- Target enablement and every failure path are audited.
- No Pi token or target password is copied into Git or Flutter assets.
- Frames are bounded, redacted, and non-persistent by default.
- No authentication, input, clipboard, file, device, audio, or shell action
  occurs.
- BrainConnect and BoxBrain validation passes.
