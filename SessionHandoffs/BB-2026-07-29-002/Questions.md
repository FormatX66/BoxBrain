# Questions

1. Should the first frame observer use a credential broker that returns a
   short-lived controlled session, or should a separate lab-only credential be
   injected directly into the native helper process?
2. What frame cadence and maximum resolution are sufficient for the first
   observation experiment?
3. Should every observation run restore the clean checkpoint automatically
   after operator approval, or should restoration remain a separate explicit
   step?
4. When may the detached answer ISO and encrypted lab credential be removed
   after recovery needs are reviewed?
5. Should the workstation receive more memory before frame work, or should the
   VM remain at a 1 GiB startup allocation with dynamic growth?
