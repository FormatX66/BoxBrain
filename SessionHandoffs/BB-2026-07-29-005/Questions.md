# Questions

1. Should the first live connector use FreeRDP for every operation, or combine
   FreeRDP input/clipboard with the existing Pi-only SSH boundary for shell?
2. Which runtime credential provider should the Pi use: a root-owned
   credential socket, systemd credentials, or an external secrets service?
3. Which first live action gives the clearest verification with least state
   change: pointer move, harmless key chord, or clipboard read?
4. What bounded evidence proves a pointer or key action without retaining a
   full raw desktop frame?
5. Should target disablement cancel queued operations or leave them queued for
   explicit operator review?
6. When may the detached answer ISO containing the one-time lab password be
   removed?
