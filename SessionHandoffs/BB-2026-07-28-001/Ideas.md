# Ideas — BB-2026-07-28-001

- Add a workspace manifest after remote repository URLs are known so local
  paths are portable across machines.
- Run `Admin/validate_repository.py` in CI on every BoxBrain change.
- Generate a read-only ecosystem status page from project indexes rather than
  maintaining a second dashboard document.
- Use stable decision IDs across projects and link project ADRs instead of
  copying them.
- Add machine-readable project metadata only after the Markdown governance
  model proves stable.

Ideas are not commitments. Promote an idea through a recorded decision before
implementation.
