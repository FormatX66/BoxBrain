# Ideas

- Add a dashboard QR code that reveals AP credentials only after local operator
  authentication and never writes the key to logs.
- Support a second Wi-Fi adapter for an independent AP channel and radio.
- Add a bounded AP client-list view with MAC addresses minimized by default.
