# Questions

1. Should the next native operation be scrolling, shell, or clipboard?
2. Should the upgrade preflight inspect effective authenticated health,
   systemd drop-in state, or both?
3. Should the native build move to FreeRDP 3.26, or should 3.15 build plus 3.26
   on-host gates remain the supported compatibility contract?
4. When should the operator approve deleting or merging retired checkpoint
   `clean-linked-2026-07-29`?
5. Should the click-and-type Notepad proof become a fixed automated regression
   scenario?
