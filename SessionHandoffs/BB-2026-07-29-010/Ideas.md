# Ideas

- Add a controller installer preflight that compares effective health with the
  systemd drop-in before any file upload or service stop.
- Package the visible `FINAL |CLICK|BoxBrain` workflow as a repeatable
  mouse-and-keyboard acceptance test.
- Add a small lab-only target app with deterministic controls and coordinates
  so frame evidence is less dependent on Notepad layout.
- Score transport success and observed-state success as separate benchmark
  dimensions.
- Evaluate scrolling before clipboard or shell because it expands control
  while adding less data-exposure risk.
