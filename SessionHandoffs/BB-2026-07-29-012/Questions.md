# Questions

1. Should FreeRDP 3.15 builds plus exact Pi 3.26 on-host gates remain the
   supported compatibility contract after the scrolling proof?
2. Should vertical scrolling be promoted first, with horizontal scrolling
   retained as tested source until a deterministic horizontal target exists?
3. Should the Notepad click-and-type workflow become a fixed automated
   acceptance scenario?
4. Which BoxBrain project should follow BrainConnect in priority?
5. What is Arkmatx's purpose and where are its current files or repository?
