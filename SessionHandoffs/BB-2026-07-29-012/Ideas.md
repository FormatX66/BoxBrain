# Ideas

- Generate forty numbered lines in Notepad, capture a narrow bounded region,
  scroll down one notch at the text area, and compare the visible line numbers.
- Add a deterministic lab-only scrolling window with fixed coordinates and a
  visible position counter.
- Record wheel direction, step count, pointer location, before-frame hash, and
  after-frame hash as a reusable benchmark record.
- Promote vertical wheel input first if the exact Pi gate cannot prove
  horizontal-wheel capability negotiation.
- Keep shell and clipboard behind separate decisions because they expand
  execution and data-transfer boundaries.
